#include "gtest\gtest.h"
#include "Challenge02.h"
#include "Challenge02.cpp"

// ----------------------------------------------------------------------
// Challenge01 - Binary Search
// ----------------------------------------------------------------------
TEST(testChallenge02, Handle) {
	Challenge02 c02;

	vector<int> binaryTreeValues = {1, 2, 3, 4, 5, 6, 7, 8, 9}; 

	// TODO: Create test cases that test 2 functions with different types of binary tree 
	// => Create a binary tree class that has constructor that takes in array, or vector 
	// => Constructors also take into account different traversal ways of binary tree
	// => List out all possible binary trees I can create 
	// => Optional: Create functions that traverse the binary trees in different ways 
	// => This class testing the depth of binary tree => pass in all types of binary tree for the 2 functions

	/*vector<int> testVector = { -1000, -285, -99, -2, 1, 7, 88, 190, 200, 999 };
	EXPECT_EQ(0, c01.search(testVector, -1000, 0));
	EXPECT_EQ(6, c01.search(testVector, 88, 0));
	EXPECT_EQ(1, c01.search(testVector, -285, 0));
	EXPECT_EQ(9, c01.search(testVector, 999, 0));*/
}
